<%= form_for(@rating_type) do |f| %>
  <% if @rating_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating_type.errors.count, "error") %> prohibited this rating_type from being saved:</h2>

      <ul>
      <% @rating_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rate_id %><br>
    <%= f.text_field :rate_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
